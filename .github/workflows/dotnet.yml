name: CI Build

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**/*.md'
      - '**/*.gitignore'
      - '**/*.gitattributes'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

env:
  DOTNET_NOLOGO: true                     # Disable the .NET logo
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true # Disable the .NET first time experience
  DOTNET_CLI_TELEMETRY_OPTOUT: true       # Disable sending .NET CLI telemetry

jobs:
# MAUI Android Build
  build-android:
    runs-on: windows-2022
    environment: Dev
    name: Android Build
    steps:
       #substitute production appsettings entries to appsettings json file
      - name: App Settings Variable Substitution
        uses: microsoft/variable-substitution@v1
        with:
          files: 'Maui_Github_Actions_KeyVault_Test/Resources/raw/AppSettings.json'
        env:
          test: testWorks
    #     run: |
    #         echo "IsItMasked"
    #         echo "--------------"
    #         echo ${{secrets.SECRET1}}
    #         echo "--------------"
    #         echo ${{secrets.SECRET1}} | sed 's/./& /g'    
    #         echo "env stuff"
    # 


   #   - name: Checkout
   #     uses: actions/checkout@93ea575cb5d8a053eaa0ac8fa3b40d7e05a33cc8
#
   #   - name: Setup .NET 6
   #     uses: actions/setup-dotnet@4d4a70f4a5b2a5a5329f13be4ac933f2c9206ac0
   #     with:
   #       dotnet-version: 6.0.x
   #       
   #   - name: Install MAUI Workload
   #     run: dotnet workload install maui --ignore-failed-sources
#
   #   - name: Restore Dependencies
   #     run: dotnet restore Maui_Github_Actions_KeyVault_Test/Maui_Github_Actions_KeyVault_Test.csproj
#
   #   - name: Build MAUI Android
   #     run: dotnet publish Maui_Github_Actions_KeyVault_Test/Maui_Github_Actions_KeyVault_Test.csproj -c Release -f net6.0-android --no-restore
#
   #   - name: Upload Android Artifact
   #     uses: actions/upload-artifact@3cea5372237819ed00197afe530f5a7ea3e805c8
   #     with:
   #       name: mauibeach-android-ci-build
   #       path: Maui_Github_Actions_KeyVault_Test/bin/Release/net6.0-android/*Signed.a*

